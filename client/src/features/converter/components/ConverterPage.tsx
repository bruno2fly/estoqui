/**
 * REMOVED FEATURE — the File Converter is gone: every upload surface
 * (Inventory, Vendors) now accepts any file directly and converts internally.
 * This stub only exists because the workspace bridge can't delete files;
 * remove the whole features/converter folder with:  git rm -r client/src/features/converter
 */
export function ConverterPage() {
  return null
}
